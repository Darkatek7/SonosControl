@if (Visible)
{
    <div class="app-dialog-backdrop" role="presentation" @onclick="CancelAsync">
        <section class="app-dialog app-dialog--confirm"
                 role="alertdialog"
                 aria-modal="true"
                 aria-labelledby="@_titleId"
                 aria-describedby="@_messageId"
                 @onclick:stopPropagation="true">
            <div class="app-dialog__header">
                <div>
                    <span class="spotify-eyebrow">Confirmation</span>
                    <h2 id="@_titleId">@Title</h2>
                </div>
                <button type="button" class="btn app-dialog__close" @onclick="CancelAsync" aria-label="Close dialog">
                    <Icon Name="x" Size="18" />
                </button>
            </div>
            <div class="app-dialog__body">
                <p id="@_messageId" class="app-dialog__message">@Message</p>
            </div>
            <div class="app-dialog__actions">
                <button type="button" class="btn btn-outline-secondary" @onclick="CancelAsync">Cancel</button>
                <button type="button"
                        class="btn @(Dangerous ? "btn-danger" : "btn-primary")"
                        @onclick="ConfirmAsync"
                        autofocus>
                    @ConfirmLabel
                </button>
            </div>
        </section>
    </div>
}

@code {
    private readonly string _titleId = $"confirm-title-{Guid.NewGuid():N}";
    private readonly string _messageId = $"confirm-message-{Guid.NewGuid():N}";

    [Parameter] public bool Visible { get; set; }
    [Parameter] public string Title { get; set; } = "Confirm action";
    [Parameter] public string Message { get; set; } = string.Empty;
    [Parameter] public string ConfirmLabel { get; set; } = "Confirm";
    [Parameter] public bool Dangerous { get; set; }
    [Parameter] public EventCallback OnConfirm { get; set; }
    [Parameter] public EventCallback OnCancel { get; set; }

    private Task ConfirmAsync() => OnConfirm.InvokeAsync();
    private Task CancelAsync() => OnCancel.InvokeAsync();
}
