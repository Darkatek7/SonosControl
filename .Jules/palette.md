## 2024-05-22 - Replacing Emoji with Icons
**Learning:** Emojis are inconsistent across platforms (e.g., headless Linux environments used for testing) and often lack accessibility features by default.
**Action:** Prefer using icon libraries (like FontAwesome) with explicit `aria-label` attributes over raw emojis for interactive elements to ensure consistent rendering and accessibility.
